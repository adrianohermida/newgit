deu erro, quando uso o payload de teste no supabase no trf1 funciona este:

{
      "numeroProcesso": "00008323520184013202"
    }

se tento outeros ele nao consegue concluir fvioa edge function, ajustar

vreveja o codigo que funciona e aprimorar para incluir todos os endpoint:


import { serve } from "https://deno.land/std@0.224.0/http/server.ts";

serve(async (req) => {
  try {
    const { numeroProcesso } = await req.json();

    const apiKey = Deno.env.get("DATAJUD_API_KEY"); // pega do secret

    if (!apiKey) {
      return new Response(JSON.stringify({ error: "API Key não configurada" }), {
        headers: { "Content-Type": "application/json" },
        status: 500,
      });
    }

    const url = "https://api-publica.datajud.cnj.jus.br/api_publica_trf1/_search";
    const payload = JSON.stringify({
      query: { match: { numeroProcesso } }
    });

    const response = await fetch(url, {
      method: "POST",
      headers: {
        "Authorization": ApiKey ${apiKey},
        "Content-Type": "application/json"
      },
      body: payload
    });

    const data = await response.text();

    return new Response(data, {
      headers: { "Content-Type": "application/json" },
      status: 200
    });
  } catch (err) {
    return new Response(JSON.stringify({ error: String(err) }), {
      headers: { "Content-Type": "application/json" },
      status: 400,
    });
  }
});
ESTÁ CORRETO?

// Adicione isso no topo do seu index.ts
import { serve } from "https://deno.land/std@0.168.0/http/server.ts"

const corsHeaders = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
}

serve(async (req) => {
  // 1. Tratar Preflight Request (OPTIONS)
  if (req.method === 'OPTIONS') {
    return new Response('ok', { headers: corsHeaders })
  }


  try {
    const { numeroProcesso } = await req.json();

    const apiKey = Deno.env.get("DATAJUD_API_KEY"); // pega do secret

    if (!apiKey) {
      return new Response(JSON.stringify({ error: "API Key não configurada" }), {
        status: 500,
        headers: {
          "Content-Type": "application/json",
          "Access-Control-Allow-Origin": "*",
          "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
        },
      });
    }

    const url = "https://api-publica.datajud.cnj.jus.br/api_publica_trf1/_search";
    const payload = JSON.stringify({
      query: { match: { numeroProcesso } }
    });

    const response = await fetch(url, {
      method: "POST",
      headers: {
        "Authorization": ApiKey ${apiKey},
        "Content-Type": "application/json"
      },
      body: payload
    });

    const data = await response.text();

  
    // 2. Retornar com Headers CORS
    return new Response(JSON.stringify(data), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
    })
  } catch (error) {
    return new Response(JSON.stringify({ error: error.message }), {
      headers: { ...corsHeaders, 'Content-Type': 'application/json' },
      status: 400,
    })
  }
});
COMO CONFIGURAR O HEADER cros NO EDGE FUNCTIONS NO SUPABASE
if (req.method === 'OPTIONS') {
  return new Response('ok', {
    headers: {
      ...corsHeaders,
      'Access-Control-Allow-Methods': 'GET, POST, OPTIONS',
    },
  });
}
como usar o railways para fazer a consulta ao datajud no lugar do edge function ou utilizar como ponte para acionar o datajud usando o edge function?
Criar DataJudGateway (Railway) oficial
Me de a função deno TypeScript JavaScript para executar o cnjparse no base44
Em deno me de instruções completas da integração datajud para obter os detalhes do processo e movimentos 
Forneça instruções de exemplos de pesquisa no datajud
Faça teste real do endpoint do tjsp do processo 1033458-06.2023.8.26.0577 e ne de aqui no chat a resposta completa dos detalhes e movimentos recebidos 
Qual a instrução para montar a base de dados das tabelas de movimentos, classes e assuntos do cnj datajud? Elastic