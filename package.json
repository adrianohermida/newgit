{
  "name": "newgit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:pages": "npm run build && npx wrangler pages dev out --project-name=newgit-pages",
    "build": "next build",
    "build:pages": "npm run build",
    "deploy:pages": "npm run build && npx wrangler pages deploy out --project-name=newgit-pages",
    "deploy:hmadv-ai": "powershell -ExecutionPolicy Bypass -File scripts/deploy-hmadv-process-ai.ps1",
    "setup:hmadv-ai": "powershell -ExecutionPolicy Bypass -File scripts/setup-hmadv-process-ai.ps1",
    "start": "node .next/standalone/server.js",
    "import:hmadv-billing": "node scripts/import-hmadv-billing-csv.js",
    "sync:freshsales-contacts": "node scripts/sync-freshsales-contacts.js",
    "gh-pages": "GITHUB_PAGES=true next build && rm -rf docs && mkdir docs && cp -r out/* docs/ && touch docs/.nojekyll",
    "test:integration": "node scripts/test-integration.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.0",
    "framer-motion": "^12.35.2",
    "googleapis": "^171.4.0",
    "lucide-react": "^0.577.0",
    "next": "latest",
    "nodemailer": "^8.0.2",
    "react": "latest",
    "react-dom": "latest",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.0.0",
    "wrangler": "^4.76.0"
  }
}
