def add_dir(path: str) -> str:
    """Comando real: cria um diretório no caminho especificado (simulado)."""
    # Aqui apenas simulamos, pois não queremos efeitos colaterais reais.
    return f"Diretório '{path}' criado com sucesso (simulado)."
